export class Flyweight {
    textColor;
    backgroundColor;
    height;
    width;

    constructor(textColor, backgroundColor, height, width) {
        this.textColor = textColor;
        this.backgroundColor = backgroundColor;
        this.height = height;
        this.width = width;
    }
}